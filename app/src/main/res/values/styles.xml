<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:actionBarStyle">@style/MyActionBarTheme</item>
        <item name="actionBarTabTextStyle">@style/tabcolor</item>
        <item name="android:windowActionBarOverlay">true</item>

        <!--<item name="android:actionBarTabTextStyle">@style/tabcolor</item>-->
    </style>
    <style name="MyActionBarTheme" parent="@android:style/Widget.ActionBar.TabBar">
        <!--<item name="android:background">@drawable/actionbar_tab_bg</item>-->
        <item name="android:paddingLeft">32dp</item>
        <item name="android:paddingRight">32dp</item>
        <item name="android:background">#ffa500</item>
        <item name="android:elevation">8dp</item>
    </style>
    <style name="tabcolor" parent="@android:style/Widget.Holo.Light.ActionBar.TabText">
        <item name="android:textColor">@android:color/background_light</item>
    </style>
</resources>
